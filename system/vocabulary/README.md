# Vocabulary

The private full vocabulary database is not included. This public export keeps:

- the Obsidian Base dashboard schema
- a small sanitized sample vocabulary set under `examples/vocab/`
- the vocabulary-maintenance skill under `codex-skills/jp-vocab-maintainer/`

